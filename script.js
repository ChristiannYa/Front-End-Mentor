const button = document.querySelector('');
const itemName = document.querySelector('.item-name');
const itemPrice = document.querySelector('.item-price');

let count = 0;
let itemTotal = 0;
let total = 0;

const getItemTotal = (itemTotal) => {
    itemPrice
}